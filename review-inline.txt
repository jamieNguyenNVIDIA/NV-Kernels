PR #394 review notes — workqueue: sharded cache affinity scope

Verdict: no regressions found.
State:   PR was CLOSED on 2026-04-28 (merged manually by nvmochs after
         Acked-by from jamieNguyenNVIDIA and nvmochs).

Commits as posted on the PR (oldest first):

  2ff78cba0bc8 workqueue: fix parse_affn_scope() prefix matching bug
  441c719f4915 workqueue: fix typo in WQ_AFFN_SMT comment
  bde0170b8d61 workqueue: add WQ_AFFN_CACHE_SHARD affinity scope
  5241fff6384a workqueue: set WQ_AFFN_CACHE_SHARD as the default affinity
               scope
  11f929c67404 tools/workqueue: add CACHE_SHARD support to wq_dump.py
  5e368f829353 workqueue: add test_workqueue benchmark module
  9a7c8b624f71 docs: workqueue: document WQ_AFFN_CACHE_SHARD affinity scope
  d2e0836fb559 workqueue: avoid unguarded 64-bit division
  911b93df5e04 workqueue: validate cpumask_first() result in
               llc_populate_cpu_shard_id() (deep-dived per review-core.md)

Same commits landed on 26.04_linux-nvidia-bos as

  b98c9681040c c9fe0bd9ff5c 9536794c2fd7 a8b46e7d7adc 78dfce3e243e
  1e0bcbcb6c8f d9a0944605cf 93a1d393dc86 945602e4d4c2

byte-for-byte identical to the PR head (the SHA difference is just the
re-cherry-pick onto the latest base).


Cherry-pick / backport audit (clean)
====================================

All nine commits cite a "(cherry picked from commit <SHA>)" trailer.
Every cited upstream SHA was looked up:

  PR sha           Upstream sha  In upstream/linux  Subject match
  -------------    ------------  -----------------  -------------
  2ff78cba0bc8     1abaae9b38a8        yes              yes
  441c719f4915     9dc42c907028        yes              yes
  bde0170b8d61     5920d046f7ae        yes              yes
  5241fff6384a     4cdc8a7389d5        yes              yes
  11f929c67404     738390a5321c        yes              yes
  5e368f829353     24b2e73f9700        yes              yes
  9a7c8b624f71     41e3ccca00b3        yes              yes
  d2e0836fb559     c6890f36fc49        yes              yes
  911b93df5e04     76af54648899        yes              yes

All upstream SHAs exist in mainline 7.1, every subject matches, and
every diff is identical modulo line-number drift in hunk headers — no
semantic edits during the cherry-picks.


Top commit (911b93df5e04)
=========================

REACHABILITY: confirmed. precompute_cache_shard_ids() is __init and
runs once during workqueue topology init at every boot, walking every
LLC pod and calling llc_populate_cpu_shard_id() for each.

The change adds a bounded-leader guard:

    leader = cpumask_first(sibling_cpus);
    if (WARN_ON_ONCE(leader >= nr_cpu_ids))
            continue;
    cpu_shard_id[c] = cpu_shard_id[leader];

The commit message correctly identifies this as a -Warray-bounds
false-positive suppressor that only manifests on UP configs (NR_CPUS=1
makes cpu_shard_id[] a single-element int[1] and the compiler can't
prove cpumask_first(sibling_cpus) is always 0). The invariant — c is
in sibling_cpus, so cpumask_first() returns a valid CPU — holds; the
WARN can never legitimately fire.

If the WARN ever did fire, the "continue" leaves cpu_shard_id[c] at
its zero-init value (the array is __initdata static), which would put
that CPU in shard 0 — degraded but not unsafe.

No regressions.

